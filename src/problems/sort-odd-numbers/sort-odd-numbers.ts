/*
Write a function that sorts only the odd numbers
in an array while leaving even numbers in their
original positions.

Example:
Input:
[5, 3, 2, 8, 1, 4]

Output:
[1, 3, 2, 8, 5, 4]

Explanation:
- Odd numbers (5, 3, 1) become (1, 3, 5)
- Even numbers stay where they are
*/

export function sortOddNumbers(arr: number[]): number[] {
  // your code here
  return [];
}
